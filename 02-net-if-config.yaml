network:
  version: 2
  ethernets:
    [NET_IF_NAME]:
      match:
        macaddress: [NET_IF_MACADDRESS]
      set-name: [NET_IF_NAME]
      mtu: [NET_IF_MTU]
      dhcp4: yes
      dhcp6: no
      dhcp4-overrides:
        use-dns: no
      nameservers:
        addresses: [127.0.0.1]
